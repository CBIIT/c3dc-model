name: Model Validate and Build
on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  validate-and-build:
    uses: CBIIT/bento-workflows/.github/workflows/model-test-and-deploy.yml@v1.1.4.2
    with:
      model_files: |
        c3d-model.yml c3d-model-props.yml terms.yaml
      model_prefix: c3d-model
      event_name: ${{ github.event_name }}
